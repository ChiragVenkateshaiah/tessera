"""Runtime configuration.

Loaded from environment / .env via pydantic-settings. Implemented in a
later task once the modules that need config (embedding, store,
generation) exist. See CLAUDE.md — no hardcoded paths or secrets.
"""
